<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:int="http://www.springframework.org/schema/integration"
	xmlns:int-file="http://www.springframework.org/schema/integration/file"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:int-aws="http://www.springframework.org/schema/integration/aws"
	xsi:schemaLocation="http://www.springframework.org/schema/integration http://www.springframework.org/schema/integration/spring-integration.xsd
		http://www.springframework.org/schema/integration/file http://www.springframework.org/schema/integration/file/spring-integration-file.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/integration/aws http://www.springframework.org/schema/integration/aws/spring-integration-aws.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

	<context:property-placeholder />

	<bean id="credentials" class="com.amazonaws.auth.BasicAWSCredentials">
		<constructor-arg name="accessKey" value="${accessKey}"/>
		<constructor-arg name="secretKey" value="${secretKey}"/>
	</bean>

    <bean id="region" class="com.amazonaws.regions.Region" factory-method="getRegion">
        <constructor-arg value="EU_CENTRAL_1"/>
    </bean>

	<bean id="s3"  class="com.amazonaws.services.s3.AmazonS3Client">
		<constructor-arg ref="credentials"/>
        <property name="region" ref="region"/>
	</bean>

	<int:channel id="s3channel">
		<int:interceptors>
			<int:wire-tap channel="loggit"/>
		</int:interceptors>
	</int:channel>

	<int:logging-channel-adapter id="loggit" level="INFO"
		logger-name="org.springframework.integration.samples.aws.s3"
		expression="'File Name: ' + payload.name + '(' + payload.length() + ')'"/>

	<bean id="sessionFactory" class="org.springframework.integration.aws.support.S3SessionFactory">
		<constructor-arg ref="s3"/>
	</bean>
	<beans profile="poll-s3">
		<int-aws:s3-inbound-channel-adapter
				session-factory="sessionFactory"
				local-directory="s3-local-storage" channel="s3channel" remote-directory="${remoteDirectory}"/>
		<int-file:outbound-channel-adapter channel="s3channel" directory="target/s3-out"/>

		<int:poller default="true" fixed-rate="5000"/>

	</beans>
	<beans profile="upload-to-s3">
		<int-aws:s3-outbound-channel-adapter
			s3="s3"
			bucket="${bucket}" channel="s3channel" />
	</beans>
</beans>
